#include <iostream>
using namespace std;
int main ()
{
    cout<<"Enter the length:";
    int length;
    cin>>length;
    cout<<"Enter the width:";
    int width;
    cin>>width;
    int area;
    area=length*width;
    cout<<"The area is "<<area;
    return 0;
}
